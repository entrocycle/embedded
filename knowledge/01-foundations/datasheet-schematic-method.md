# 数据手册与原理图阅读方法

数据手册和原理图不是硬件工程师专属资料。嵌入式软件里的大量问题，最后都要回到引脚、电源、时钟、复位、寄存器、时序和电气特性上解释。

## 先建立问题

不要从第一页开始顺读手册。先明确你要解决的问题：

- 芯片能否在当前电压、温度、频率下工作？
- 某个引脚能不能用作目标外设功能？
- 外设初始化需要打开哪些时钟和复位？
- 中断和 DMA 请求来自哪里？
- 协议时序、最大频率、采样时间有什么限制？
- 低功耗模式下这个外设是否还能运行或唤醒？

## 一张板子的阅读顺序

### 1. 电源树

先画出所有电源轨：

```text
12V / USB / Battery
  -> DC-DC 5V
  -> LDO 3V3
  -> LDO 1V8
  -> VDDA / VDDIO / RF / Sensor
```

检查点：

- 输入范围是否覆盖实际供电。
- DC-DC/LDO 电流能力是否满足峰值。
- 模组和无线发射时是否有足够电容。
- 模拟电源 VDDA 是否有滤波。
- 电源使能脚由谁控制，默认状态是什么。

### 2. 最小系统

MCU 或 SoC 至少要确认：

- VDD/VDDA/VBAT 等供电脚。
- NRST 和复位电路。
- BOOT/Strap 引脚默认电平。
- 外部晶振、RTC 晶振和负载电容。
- SWD/JTAG/UART 调试接口。
- 去耦电容是否靠近供电脚。

### 3. 接口和外设

按总线分组阅读：

| 总线 | 必看内容 |
|---|---|
| UART | TX/RX 是否交叉、波特率、是否有电平转换 |
| I2C | 地址、上拉、电压域、总线电容、是否有多主 |
| SPI | CPOL/CPHA、片选、最大频率、MISO 三态 |
| CAN/RS-485 | 收发器、终端电阻、方向控制、隔离 |
| USB | 差分线、VBUS 检测、ESD、时钟精度 |
| ADC | 量程、分压、输入阻抗、滤波、参考电压 |
| PWM | 驱动级、频率、死区、负载能力 |

### 4. 关键控制脚

很多问题来自“看起来不起眼”的控制脚：

- 模组 EN/RESET/BOOT。
- 传感器 INT/DRDY。
- 电源开关和负载开关。
- Flash WP/HOLD。
- SD 卡 CD/WP。
- 屏幕背光、复位、TE。

把这些脚整理成一张表，标明默认电平、控制方向和上电时序。

## 数据手册五类信息

| 类型 | 用途 |
|---|---|
| Pinout | 选引脚、查复用、查电压容忍 |
| Electrical Characteristics | 电压、电流、阈值、频率、温度 |
| Memory Map | 寄存器基地址和存储区 |
| Peripheral Chapter | 初始化顺序、寄存器、状态位 |
| Errata | 已知缺陷和规避方式 |

参考手册通常比 datasheet 更适合写驱动；datasheet 更适合查引脚、电气和封装。

## 外设阅读模板

复制下面模板，为每个新外设写一页笔记：

```text
外设：
目标功能：
挂载总线：
时钟来源：
引脚复用：
DMA 请求：
中断号：
寄存器基地址：
初始化步骤：
关键配置位：
关键状态位：
错误标志：
低功耗影响：
调试观测点：
Errata/限制：
```

## 从手册到代码

以一个 I2C 传感器为例，流程应该是：

1. 从原理图确认 I2C 控制器、SCL/SDA 引脚、上拉电阻和传感器地址。
2. 从 MCU datasheet 确认引脚复用功能。
3. 从 reference manual 确认 I2C 时钟、寄存器和中断。
4. 从传感器 datasheet 确认寄存器地址、读写时序和上电延时。
5. 写最小轮询读 ID。
6. 用逻辑分析仪确认地址、ACK 和数据。
7. 再加入超时、重试、中断或 DMA。

## 常见误读

| 误读 | 正确做法 |
|---|---|
| 把 8 位 I2C 地址直接传给 7 位 API | 确认 API 接收 7 位还是左移后的 8 位地址 |
| 看到引脚支持复用就直接用 | 检查同一引脚是否和其他功能冲突 |
| 只看典型值 | 设计要看最小值、最大值和温度范围 |
| 忽略 Errata | 新芯片和复杂外设必须查 Errata |
| 默认内部上拉足够 | I2C、BOOT、复位等关键线上通常要核对外部上拉 |
| 认为波特率错一定是 UART 问题 | 也可能是系统时钟或晶振配置错 |

## Bring-up 记录模板

```text
板卡版本：
芯片型号：
电源轨测量：
复位脚状态：
BOOT 脚状态：
调试口连接：
串口输出：
外部晶振：
第一项外设：
异常现象：
下一步验证：
```

## 练习

1. 找一块开发板原理图，把所有 I2C 设备地址和上拉电阻整理出来。
2. 选一个传感器，按“外设阅读模板”写一页笔记。
3. 把一次启动失败定位过程整理成 bring-up 记录。
